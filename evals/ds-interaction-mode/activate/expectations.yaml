# Smoke: activating the mode must exit zero and produce non-empty output;
# no expectation list applies (FR-10.c).
task: "Run /ds-interaction-mode to activate interaction mode for this session."
tier: smoke
